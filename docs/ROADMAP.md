# План разработки — Genshin Code Monitor → HoYo Code Monitor

## Статус: v1 Genshin в стабилизации, мульти-игра — после релиза

---

## Фаза 0. Заморозка v1 (текущая, ~90% готово)

**Цель:** стабильный релиз Genshin без новых фич.

- [x] Мониторинг + автопогашение end-to-end (доказано живьём)
- [x] 16 пресетов источников (10 активных)
- [x] Web UI: 5 страниц, 6 языков, тумблы, фильтры, пагинация
- [x] Трей, автозапуск, single-instance, шифрование куков
- [x] 223 теста, CHANGELOG, 6 README
- [ ] Вписать ссылки автора в `src/author.py` (ждём данные)
- [ ] Финальный smoke: свежий ПК → install → add account → login → redeem
- [ ] Тег `v1.0.0` + GitHub Release (exe + installer)

**Критерий выхода:** пользователь ставит с нуля и гасит код без участия разработчика.

---

## Фаза 1. Публикация (1–2 дня)

- [ ] Создать GitHub-репозиторий, залить main
- [ ] Включить GitHub Sponsors (после — добавить ссылку в `author.py`)
- [ ] Релиз `v1.0.0`: PyInstaller `.exe` + NSIS-инсталлятор (собрать и проверить на чистой Windows)
- [ ] README-бейджи (tests, release), скриншоты UI в README
- [ ] Объявление: Reddit r/Genshin_Impact? (осторожно с правилами), HoYoLAB, Discord

**Критерий:** релиз скачивается, устанавливается, работает.

---

## Фаза 2. Мульти-игра — ветка `multi-game` (3–5 дней)

**Цель:** одно приложение для Genshin + HSR + ZZZ (+HI3 опционально).

### 2.1. Данные (миграция v11)
- [ ] `game TEXT DEFAULT 'genshin'` в `sources`, `codes`, `accounts`
- [ ] Существующие данные мигрируют как `genshin`, ничего не теряется
- [ ] Тесты миграции (v10 → v11, откат)

### 2.2. Конфигурация игр
- [ ] `GAME_CONF` маппинг: `game_biz`, API-хост, регион, паттерн кодов, интервал
  - Genshin: `hk4e_global`, `sg-hk4e-api.hoyoverse.com`, GET
  - HSR: `hkrpg_global`, `sg-hkrpg-api.hoyoverse.com`, GET
  - ZZZ: `nap_global`, `public-operation-nap`, POST `webExchangeCdkeyRisk`
- [ ] `Redeemer` принимает `game`, ZZZ — отдельный метод (Risk API)

### 2.3. Источники
- [ ] Поле `game` у `SourceConfig` + колонка (миграция v11)
- [ ] hoyo-codes / ennead: тот же URL, другой `?game=`
- [ ] Гайд-пресеты под HSR/ZZZ (проверить живьём, как делали для Genshin)
- [ ] Фильтр кодов по игре в fetcher и UI

### 2.4. UI
- [ ] Переключатель игры в сайдбаре (как переключатель языка)
- [ ] Фильтр «игра» в таблице кодов и статистике
- [ ] Аккаунты: привязка `game` (один HoYoLAB-аккаунт → персонажи в разных играх)
- [ ] i18n-ключи для новых строк (6 языков)

### 2.5. Проверка
- [ ] Живое погашение тестового кода в HSR и ZZZ
- [ ] Полный сьют зелёный (цель: 250+ тестов)
- [ ] Релиз `v0.2.0-beta`, затем `v2.0.0`

**Критерий:** один трей гасит коды трёх игр, данные v1 на месте.

---

## Фаза 3. После мульти-игры (по желанию)

- [ ] Уведомления (Windows toast при новом коде / успешном погашении)
- [ ] Telegram-бот нотификатор (опционально, локальный)
- [ ] Автообновление приложения (проверка GitHub Releases)
- [ ] Экспорт/импорт БД (бэкап в один клик из UI)
- [ ] Тёмная тема Web UI
- [ ] Нативный GUI (Tauri) — только если Web UI станет тесно

---

## Принципы (зафиксированы)

1. Локально-only: ничего не покидает ПК, кроме запросов к источникам и Hoyolab API.
2. Один шаг — один коммит, сразу после зелёных тестов (`docs/WORKFLOW.md`).
3. Никаких параллельных правок одних файлов.
4. Сначала живой пруф (сеть/API), потом коммит.
5. Безопасность: куки только в Fernet, секреты — никогда в git.
